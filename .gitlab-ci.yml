pages:
  stage: deploy
  image: python:3.12-slim
  tags:
    - wsl-kiro-runner
  script:
    - pip install mkdocs-material pymdown-extensions
    - mkdocs build --site-dir public
  artifacts:
    paths:
      - public
  only:
    - main
