pages:
  stage: deploy
  tags:
    - wsl-kiro-runner
  script:
    - python3 -m venv /tmp/mkdocs-venv
    - /tmp/mkdocs-venv/bin/pip install mkdocs-material pymdown-extensions
    - /tmp/mkdocs-venv/bin/mkdocs build --site-dir public
  artifacts:
    paths:
      - public
  only:
    - main
